import { ValidColumns } from './form-grid';

/**
 * 总的栅格数
 *
 * 24 格
 */
export const TOTAL_SPAN = 24;

/**
 * 默认的栏目
 *
 * 默认就 1 栏
 */
export const DEFAULT_COLUMNS: ValidColumns = 1;

/**
 * 默认的栏目间距
 *
 * 默认 8px
 */
export const DEFAULT_GUTTER = 8;
